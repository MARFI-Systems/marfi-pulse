# Upstream OneUptime CI

These workflow files shipped with OneUptime. They are parked here so this MARFI fork does not run 25 upstream jobs on every push.

Active MARFI CI lives in `../workflows/`:
- `trivy-security.yml` on Monk `monkci-ubuntu-24.04-2`

Do not move these back without an explicit spend decision.
